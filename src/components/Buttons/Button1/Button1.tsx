import React from "react";
import styles from "./Button1.module.css";
import { clsx } from "clsx";

type Props = {};

const Button1 = (props: Props) => {
  return <div className={styles.button}>თანამშრომლის შექმნა</div>;
};

export default Button1;
